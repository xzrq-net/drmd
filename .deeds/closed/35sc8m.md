---
tier: objective
---
# Render Mermaid fenced code blocks as diagrams

Research the best integration for this localhost Markdown previewer and add
Mermaid support. Use the official browser renderer, with a pinned local bundle
so the existing single-binary Cargo/Nix builds work offline. Load it only when
Mermaid code blocks are present. Keep invalid diagram source visible and isolate
errors so other diagrams still render. Verify live reload and ordinary Markdown.

Closed: Implemented offline Mermaid rendering with isolated errors and scroll restoration; verified in Chromium and with Cargo/Nix checks.
